"""Escalator: local-only escalation chain for Pi-built bd stories.

Mirrors the work-loop SKILL.md (Step 8 escalation chain + Step 9b Deep
Research) but executes in pure Python with local Ollama endpoints (no
OpenRouter, no claude -p subscription path).
"""
